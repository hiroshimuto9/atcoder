export const A_197 = (input: string) => {
  const inputVal = input.trim();
  const inputValArray = [...inputVal];
  const output = `${inputVal[1]}${inputVal[2]}${inputVal[0]}`
  return console.log(output);
}